import React from 'react'
import { Tooltip } from '../../../../design-system'

import './Toolbar.css'

function Toolbar({ UserInterface }) {
    // The toolbar is rendered by jQuery — this component adds React enhancements
    // Tool buttons are managed by ToolController_ and added to #toolbar via jQuery
    // We just add an overlay class for styling hooks
    return null
}

export default Toolbar
